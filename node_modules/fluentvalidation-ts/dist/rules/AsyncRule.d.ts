import { CoreRule } from './CoreRule';
import { AsyncValueValidator } from 'valueValidator/AsyncValueValidator';
import { ValueValidationResult } from '../ValueValidationResult';
export declare class AsyncRule<TModel, TValue> extends CoreRule<TModel> {
    private readonly asyncValueValidator;
    constructor(asyncValueValidator: AsyncValueValidator<TModel, TValue>);
    validateAsync: (value: TValue, model: TModel) => Promise<ValueValidationResult<TValue>>;
}
