import { AsyncRule } from './AsyncRule';
export declare class MustAsyncRule<TModel, TValue> extends AsyncRule<TModel, TValue> {
    constructor(definition: ((value: TValue, model: TModel) => Promise<boolean>) | {
        predicate: (value: TValue, model: TModel) => Promise<boolean>;
        message: string | ((value: TValue, model: TModel) => string);
    } | Array<((value: TValue, model: TModel) => Promise<boolean>) | {
        predicate: (value: TValue, model: TModel) => Promise<boolean>;
        message: string | ((value: TValue, model: TModel) => string);
    }>);
}
