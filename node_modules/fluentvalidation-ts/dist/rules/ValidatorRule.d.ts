import { IValidator } from '../IValidator';
import { Rule } from './Rule';
export declare class ValidatorRule<TModel, TValue> extends Rule<TModel, TValue> {
    constructor(validatorProducer: (model: TModel) => IValidator<TValue>);
}
