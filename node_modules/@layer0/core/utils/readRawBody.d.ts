import Request from '../router/Request';
/**
 * Reads the request body and returns it. It can only be used once per request.
 *
 * @param req
 * @return {Promise}
 */
export default function readRawBody(req: Request): Promise<unknown>;
