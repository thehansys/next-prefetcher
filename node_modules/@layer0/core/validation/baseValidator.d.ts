import { Validator } from 'fluentvalidation-ts';
import { BaseResponseWriter } from '../router/BaseResponseWriter';
/**
 * Base class for all validators needed for BaseResponseWriter calls.
 */
export declare class BaseValidator<T> extends Validator<T> {
    validateWithThrow(value: T, validationName: keyof BaseResponseWriter): void;
}
