"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const chalk_1 = __importDefault(require("chalk"));
const fluentvalidation_ts_1 = require("fluentvalidation-ts");
/**
 * Base class for all validators needed for BaseResponseWriter calls.
 */
class BaseValidator extends fluentvalidation_ts_1.Validator {
    validateWithThrow(value, validationName) {
        const res = this.validate(value);
        if (Object.keys(res).length > 0) {
            const errors = JSON.stringify(res, null, 4);
            throw new Error(chalk_1.default.redBright(`\nGiven parameters for "${validationName}" are invalid.\n${errors}}`));
        }
    }
}
exports.BaseValidator = BaseValidator;
