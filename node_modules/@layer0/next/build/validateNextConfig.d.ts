/**
 * Ensures that next.config.js exists and sets target: 'serverless'.
 * @param {String} appDir The path to Layer0 app's root directory
 */
export default function validateNextConfig(appDir: string): void;
